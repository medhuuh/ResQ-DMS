import React from 'react';
import { NavLink } from 'react-router-dom';
import {
    LayoutDashboard,
    Home,
    AlertTriangle,
    Map,
    Tent,
    ShieldCheck,
    Users,
    Package,
    UserX,
    HandHeart,
    BarChart3,
    Settings,
    LogOut
} from 'lucide-react';
import clsx from 'clsx';

const Sidebar = () => {
    const navItems = [
        { name: 'Home', icon: Home, path: '/' },
        { name: 'Live Status', icon: AlertTriangle, path: '/live-status' },
        { name: 'Dashboard', icon: LayoutDashboard, path: '/admin/dashboard' },
        { name: 'Alerts', icon: AlertTriangle, path: '/admin/alerts' },
        { name: 'Risk Map', icon: Map, path: '/admin/map' },
        { name: 'Camps', icon: Tent, path: '/camps' },
        { name: 'Safe Homes', icon: ShieldCheck, path: '/safe-homes' },
        { name: 'Refugees', icon: Users, path: '/refugees' },
        { name: 'Resources', icon: Package, path: '/resources' },
        { name: 'Missing', icon: UserX, path: '/missing-persons' },
        { name: 'Volunteers', icon: HandHeart, path: '/volunteers' },
        { name: 'Analytics', icon: BarChart3, path: '/analytics' },
    ];

    return (
        <aside className="h-screen w-64 bg-surface text-white flex flex-col fixed left-0 top-0 z-50 transition-all duration-300 border-r border-white/10">
            <div className="p-6">
                <h1 className="text-2xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-neon to-primary">
                    ResQ-DMS
                </h1>
                <p className="text-xs text-gray-400 mt-1">Disaster Management</p>
            </div>

            <nav className="flex-1 px-4 space-y-2 overflow-y-auto custom-scrollbar">
                {navItems.map((item) => (
                    <NavLink
                        key={item.path}
                        to={item.path}
                        className={({ isActive }) =>
                            clsx(
                                "flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-200 group",
                                isActive
                                    ? "bg-primary text-white shadow-lg shadow-primary/20 font-bold"
                                    : "text-gray-400 hover:bg-black/20 hover:text-white"
                            )
                        }
                    >
                        <item.icon className="w-5 h-5" />
                        <span className="font-medium text-sm">{item.name}</span>
                    </NavLink>
                ))}
            </nav>

            <div className="p-4 border-t border-white/10">
                <button className="flex items-center gap-3 px-4 py-3 w-full rounded-xl text-gray-400 hover:bg-black/20 hover:text-white transition-all">
                    <Settings className="w-5 h-5" />
                    <span className="font-medium text-sm">Settings</span>
                </button>
            </div>
        </aside>
    );
};

export default Sidebar;
